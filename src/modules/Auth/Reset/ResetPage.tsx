import React from 'react';
import { Center } from '@mantine/core';
import ResetForm from './Form/ResetFrom';

const ResetPage = () => {
  return (
    <Center style={{ height: '100%' }}>
      <ResetForm />
    </Center>
  );
};

export default ResetPage;
